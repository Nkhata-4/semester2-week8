
#include <stdbool.h>
#include <math.h>

#include "shapes.h"

Point makePoint( float x, float y ) {
    Point new;
    p1.x=2.3;
    p1.y=1.2;

    p2.x=2.3;
    p2.y=3.4;
    // implementation
    abs(lineLength(p1,p2))<1.0e-6
    return new;
}

Line makeLine( Point p1, Point p2 ) {
    Line newLine;
    distance = distance = sqrt((x2-x1)^2 + (y2-y1)^2)
    newLine1=(p1, p2);
    newLine2=(p2, p3);
    return newLine;
}


// complete other functions below
// - start with stubs as above
// - compile regularly to test syntax
// - test functions by calling them from main()
